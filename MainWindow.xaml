<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClimateSim"
        mc:Ignorable="d"
        Title="ClimateSim - Schritt 1" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Links: Temperaturkarte -->
        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Padding="5">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="ImgMap"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="Fant"
                       RenderOptions.EdgeMode="Aliased" />
            </ScrollViewer>
        </Border>
        
        <!-- Rechts: Steuerungselemente -->
        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Stretch">
            <TextBlock Text="Klimamodell - Schritt 1"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,10" />
            <TextBlock Text="Raster-Auflösung:" FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                <TextBlock Text="Breite (Lon):" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtWidth" Width="60" Margin="5,0,10,0" Text="360"/>
                <TextBlock Text="Höhe (Lat):" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtHeight" Width="60" Margin="5,0,0,0" Text="180"/>
            </StackPanel>

            <TextBlock Text="Temperaturbereich (für Farbkodierung):" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                <TextBlock Text="Min [°C]:" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtTempMin" Width="60" Margin="5,0,10,0" Text="-50"/>
                <TextBlock Text="Max [°C]:" VerticalAlignment="Center"/>
                <TextBox x:Name="TxtTempMax" Width="60" Margin="5,0,0,0" Text="40"/>
            </StackPanel>

            <Button x:Name="BtnGenerate"
                    Content="Temperaturfeld initialisieren"
                    Height="35"
                    Margin="0,10,0,0"/>

            <Button x:Name="BtnStep"
                    Content="1 Simulationsschritt (z.B. 0,5 Jahre)"
                    Height="35"
                    Margin="0,10,0,0" />

            <TextBlock Text="Automatische Simulation:"
                      FontWeight="SemiBold"
                      Margin="0,20,0,5" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="dt pro Tick [Jahre]:" VerticalAlignment="Center" />
                <TextBox x:Name="TxtDtYears" Width="60" Margin="5,0,10,0" Text="0.2" />
                <TextBlock Text="Intervall [ms]:" VerticalAlignment="Center" />
                <TextBox x:Name="TxtIntervalMs" Width="60" Margin="5,0,0,0" Text="50" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="BtnStart"
                        Content="Start"
                        Width="80"
                        Height="30"
                        Margin="0,0,10,0" />
                <Button x:Name="BtnStop"
                        Content="Stop"
                        Width="80"
                        Height="30" />
            </StackPanel>

            <TextBlock Text="Simulationszeit:"
                       FontWeight="SemiBold"
                       Margin="0,10,0,0" />
            <TextBlock x:Name="TxtSimTime"
                       Text="0,0 Jahre"
                       Margin="0,2,0,0"
                       FontFamily="Consolas" />
        </StackPanel>
    </Grid>
</Window>
